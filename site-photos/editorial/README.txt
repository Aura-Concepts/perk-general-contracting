Stills used on a page but not part of any gallery project.

These two were rescued from hand-placed files in assets/img/ — the original
camera files are lost, so the largest surviving web rendition is now the
source. That caps their resolution (1280px and 960px), but the build owns
them now, so they get sharpening, WebP, and the same size ladder as
everything else.

If a true high-resolution original ever turns up, drop it in here under the
same filename and rebuild — everything else takes care of itself.
